# IQ200 OS v8.0.3 UPDATE ENGINE X10THINK

Adds fast database update command without forcing full SD scan.

Commands:
- `update` / `upd` / `dbupdate`: validate database and reload playlist.
- Falls back to full scan only if media.meta/media.idx/volumes are invalid.

RT Audio unchanged.
