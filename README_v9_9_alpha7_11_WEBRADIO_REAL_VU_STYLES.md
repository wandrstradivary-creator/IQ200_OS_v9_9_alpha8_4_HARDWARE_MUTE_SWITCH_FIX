# IQ200 OS v9.9-alpha7.11 — Real WebRadio VU Styles

Six visually distinct TFT VU renderers: Line, Thin, Blocks, Dots, Neon and
Center. They share the bounded 440×48 PSRAM sprite and do not add work to the
audio decoder task.

Build with `pio run -e iq200-radio`.
