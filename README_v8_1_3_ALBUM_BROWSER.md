# v8.1.3 Album Browser

Adds an album-browser screen foundation on top of the existing indexed database engine. This version does not rescan SD for album navigation; it prepares the UI and command layer for album.idx / cover cache in the next step.

Commands:
- albums
- album
- alb

RT Audio unchanged.
