# IQ200 OS v9.2-alpha13.1 VU SEGMENT PARSER

- Strict parsing for `vuseg` arguments.
- Valid range remains 4..24.
- Added `vuseg +` and `vuseg -` with wrap-around.
- Invalid/garbled values are rejected without changing the current setting.
