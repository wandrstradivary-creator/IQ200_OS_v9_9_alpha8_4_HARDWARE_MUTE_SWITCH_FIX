# IQ200 OS v7.3.2 SMART PLAYLIST X10THINK

Changes:
- Added natural aliases: `scan -> plscan`, `list -> pl`, `info -> minfo`, `next/prev`.
- Added `playlistListRequest` handled on Core0, so `pl/list` prints the current playlist with `>` marker.
- `plscan/scan` now prints count and current track in a clearer format.
- UI Media footer/help updated to v7.3.2.
- RT Audio path unchanged from stable v7.3.1.

Test commands:
```text
scan
list
next
info
play
stop
```
