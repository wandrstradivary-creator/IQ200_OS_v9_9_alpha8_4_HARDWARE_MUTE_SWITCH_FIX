# IQ200 OS v7.7.1 FIND UI POLISH

Changes:
- Added `findall <text>` for extended DB search.
- `find <text>` default limit changed to 16 compact results.
- Search output is cleaner: numbered result, codec tag, short title, full path.
- Search remains SD-database based: `media.idx + media_###.db`.
- RT Audio untouched.
