# IQ200 OS v7.4.1 SCAN RESET X10THINK

Fixes scan screen startup state:

- scan/rescan starts at `00:00:00`.
- files/folders/tracks counters start from `0`.
- MP3/FLAC/WAV counters start from `0`.
- current path is reset to `Waiting...`.
- ETA displays `--:--` until progress data is valid.
- RT Audio unchanged.
